package comment

import (
	"github.com/gofiber/fiber/v2"
)

// router에 들어가는 함수
func SetDexEventApis(route fiber.Router) {
	// h := handler.NewDexEventHandler()

	// // 도감 목록
	// route.Get(constant.GetDexEventPath().DexEvent.GetDexEvent, h.GetDexEvent)
	// // 도감 등록
	// route.Post(constant.GetDexEventPath().DexEvent.CreateDexEvent, h.CreateDexEvent)
}
